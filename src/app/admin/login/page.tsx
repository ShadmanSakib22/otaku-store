import { PagePlaceholder } from "@/components/ui";

export default function AdminLoginPage() {
  return (
    <PagePlaceholder
      title="Admin Login"
      description="Authenticate to manage the store."
    />
  );
}