import { PagePlaceholder } from "@/components/ui";

export default function AdminInventoryPage() {
  return (
    <PagePlaceholder
      title="Inventory"
      description="Manage variant stock levels."
    />
  );
}